@using System.Linq
@model LoginViewModel
@inject SignInManager<AppUser> SignInManager

@{
    ViewData["Title"] = "Log in";
}
<!-- Breadcrumbs -->

<div class="breadcrumbs">
    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <ul>
                    <li class="home"> <a title="Go to Home Page" href="/">Trang chủ</a><span>&raquo;</span></li>
                    <li><strong>Tài khoản của tôi</strong></li>
                </ul>
            </div>
        </div>
    </div>
</div>
<!-- Breadcrumbs End -->
<!-- Main Container -->
<section class="main-container col1-layout">
    <div class="main container">
        <div class="page-content">
            <div class="account-login">
                <div class="box-authentication">
                    <form asp-route-returnurl="@ViewData["ReturnUrl"]" method="post">
                        <h4>Đăng nhập</h4>
                        <div asp-validation-summary="All" class="text-danger"></div>
                        <p class="before-login-text">Chào mừng trở lại! Đăng nhập với tài khoản của bạn</p>
                        <label for="emmail_login">Địa chỉ Email<span class="required">*</span></label>
                        <input type="text" asp-for="Email" class="form-control">
                        <label for="password_login">Mật khẩu<span class="required">*</span></label>
                        <input id="password_login" asp-for="Password" type="password" class="form-control">
                        <p class="forgot-pass"><a asp-action="ForgotPassword">Quên mật khẩu thì toang?</a></p>
                        <button type="submit" class="button">
                            <i class="icon-lock icons"></i>&nbsp; <span>Đăng nhập</span>
                        </button>
                        <label class="inline" asp-for="RememberMe">

                            <input asp-for="RememberMe" />
                            Ghi nhớ đăng nhập
                        </label>
                    </form>

                    <section>
                        <h4>Sử dụng dịch vụ khác để đăng nhập.</h4>
                        <hr />
                        @{
                            var loginProviders = (await SignInManager.GetExternalAuthenticationSchemesAsync()).ToList();
                            if (loginProviders.Count == 0)
                            {
                                <div>
                                    <p>
                                        Không có dịch vụ xác thực bên ngoài nào được định cấu hình. <a href="https://go.microsoft.com/fwlink/?LinkID=532715"></a>
                                        Chưa phát triển đăng nhập với facebook hoặc google
                                    </p>
                                </div>
                            }
                            else
                            {
                                <form asp-action="ExternalLogin" asp-route-returnurl="@ViewData["ReturnUrl"]" method="post" class="form-horizontal">
                                    <div>
                                        <p>
                                            @foreach (var provider in loginProviders)
                                            {
                                                <button type="submit" class="btn btn-default" name="provider" value="@provider.Name" title="Log in using your @provider.DisplayName account">@provider.Name</button>
                                            }
                                        </p>
                                    </div>
                                </form>
                            }
                        }
                    </section>
                </div>
                <div class="box-authentication">
                    <h4>Đăng ký</h4>
                    <p>Tạo 1 tài khoản cho riêng bạn</p>

                    <button onclick="window.location.href='/register.html'" class="button"><i class="icon-user icons"></i>&nbsp; <span>Đăng ký</span></button>
                    <div class="register-benefits">
                        <h5>Đăng ký ngay ngày hôm nay để hưởng ưu đãi :</h5>
                        <ul>
                            <li>Tăng tốc trong quá trình thanh toán</li>
                            <li>Kiểm tra hóa đơn dễ dàng</li>
                            <li>Ghi lại tất cả các giao dịch mua</li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
<!-- Main Container End -->

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}
